// Vitest global setup.
//
// Adds jest-dom matchers (`toBeInTheDocument`, etc.) and stubs
// `next/link` so component tests can render anchor children without
// the App Router runtime.

import "@testing-library/jest-dom/vitest";
